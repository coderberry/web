<div class="card">
  <div class="card-body">
    <%= simple_form_for property do |f| %>
      <%= f.input :name %>
      <%= f.input :url, placeholder: "https://" %>
      <%= f.input :description %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>
