<%= form_with(model: sponsorship, local: true) do |form| %>
  <% if sponsorship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sponsorship.errors.count, "error") %> prohibited this sponsorship from being saved:</h2>

      <ul>
      <% sponsorship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :token %>
    <%= form.text_field :token %>
  </div>

  <div class="field">
    <%= form.label :bid_amount_cents %>
    <%= form.number_field :bid_amount_cents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
